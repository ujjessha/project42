class Minutes{
    constructor(x,y){
       // mn=minute();
    }
   
    display(){
        angleMode(DEGREES);
        push();
        rotate(mnAngle);
        stroke(255,0,0);
        strokeWeight(10);
        line(0,0,100,0);
        pop();

    }
}