class Hours{
    constructor(x,y){
       // hr=hour();
    }
   
    display(){
        angleMode(DEGREES);
        push();
        rotate(hrAngle);
        stroke(255,0,0);
        strokeWeight(10);
        line(0,0,50,0);
        pop();

    }
}